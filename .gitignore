/vendor/
/composer.lock
/.phpunit.cache/
/.phpunit.result.cache
/phpunit.xml.dist.bak
.idea/
.vscode/
*.log
.env
.env.local
.php-cs-fixer.cache

# Raw K/3 Cloud metadata exports (user-provided) must NEVER be committed.
# Only the cleaned artifacts under .docs/ (produced by tools/build-metadata.php)
# are tracked: kingdee_field.standard.json / kingdee_field.custom.json / report.
/.docs/raw/
/.docs/kingdee_field.json
/.docs/kingdee_entity.json

# Generated class scaffold is a dev starting point to copy into their own project,
# not something the package ships or tracks.
/.docs/scaffold/

